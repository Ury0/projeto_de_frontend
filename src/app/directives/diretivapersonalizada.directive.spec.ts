import { DiretivapersonalizadaDirective } from './diretivapersonalizada.directive';

describe('DiretivapersonalizadaDirective', () => {
  it('should create an instance', () => {
    const directive = new DiretivapersonalizadaDirective();
    expect(directive).toBeTruthy();
  });
});
